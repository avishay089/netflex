<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background"
        android:drawable="@android:color/darker_gray" /> <!-- Empty stars color -->
    <item android:id="@android:id/secondaryProgress"
        android:drawable="@android:color/darker_gray" /> <!-- Partially filled stars -->
    <item android:id="@android:id/progress"
        android:drawable="@android:color/holo_orange_light" /> <!-- Filled stars -->
</layer-list>